import { FiltersBar } from '../../components/filters/FiltersBar';
import { ItemFilter } from "@/src/interfaces"

const zonas:ItemFilter[] = [
   {
      label: "Pilar",
      valor: "pilar"
   },
   {
      label: "Avellaneda",
      valor: "avellaneda"
   },

]

export default function Page() {

   return (
      <div className="flex flex-col my-30">
      </div>

   )
}
